<?php

declare(strict_types=1);

namespace App\Exceptions;

use RuntimeException;

/**
 * Промокод не принят: не найден, погашен, просрочен либо компания
 * под условия акции не подходит.
 *
 * Сообщение исключения показывается человеку как есть — оно объясняет
 * причину отказа. Отдельных кодов ошибок нет намеренно: на форме ввода
 * промокода различать их нечем, а «ошибка PC-04» ничего не объясняет.
 *
 * Наследует RuntimeException, чтобы выйти из транзакции с откатом:
 * возврат из замыкания DB::transaction() её коммитит.
 */
class PromoCodeRejected extends RuntimeException {}
